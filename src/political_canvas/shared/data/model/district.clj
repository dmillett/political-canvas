(ns political_canvas.shared.data.model.district)
;;
; What political district is this?
; How is the district determined?
; - which external services does it rely on
; - local configuration data
; - local database information
; District details (census)?
; - does this really matter other than for verification of potential voters
